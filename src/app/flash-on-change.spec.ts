import { FlashOnChange } from './flash-on-change';

describe('FlashOnChange', () => {
  it('should create an instance', () => {
    const directive = new FlashOnChange();
    expect(directive).toBeTruthy();
  });
});
